package com.rebook.notification.entity;

public enum NotificationType {
    REQUEST_RECEIVED,
    REQUEST_APPROVED,
    REQUEST_REJECTED,
    REQUEST_RETURNED,
    RETURN_REMINDER,
    NEW_MESSAGE,
    SYSTEM
}
